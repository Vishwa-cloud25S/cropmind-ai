# 12 — Deployment (public demo URL)

**Status:** Phase 12 · **Written:** 2026-08-11 · **Audience:** the operator deploying the public demo.
Every limit in this document is a published vendor limit or a number we measured — both are
labelled. Where a limit bites, the honest fallback is stated, not hidden.

---

## 1. What gets deployed where

| Piece | Host | Free-tier shape | What it serves |
|---|---|---|---|
| Frontend (Next.js) | **Vercel** (Hobby) | always-on static+SSR | the app UI at `https://<project>.vercel.app` |
| Backend (FastAPI API + analysis poller, **one process**) | **Render** free web service | spins down after 15 min idle | the API at `https://cropmind-demo-api.onrender.com` |
| DB (Postgres 16) | Render free Postgres **or** Supabase free | 1 GB, expires after 30 days (Render) | 16 tables |
| ML model on the public URL | **DEMO sample model only** | deterministic demo weights generated at boot | every prediction flagged `demo=true` |

**The real promoted model never leaves the operator machine** (AD-008: weights are not in git,
not on free hosting). The public URL honestly runs the clearly-flagged demo model —
`render.yaml` pins `DEMO_MODE=true` and ships no `MODEL_CHECKPOINT` — so every prediction is
flagged `demo=true`, and the endorsement reviewer sees the same golden path with DEMO banners,
exactly as Phase 15 scripts it.

### Why one process on Render (architecture deviation, stated)

Local/production truth is ADR-004 a torch-free API + a separate worker container. Render's free
tier has **no background-worker service type**, so `docker/demo.Dockerfile` +
`docker/demo-start.sh` run the poller in-process next to uvicorn. Two consequences, both stated:
- it carries torch (free instance RAM is 512 MB — measured footprint below);
- the local compose split remains the shape tested by CI; the demo image is the deployed demo only.

**Measured (this machine, 2026-08-11, Python 3.13):** API-stack imports ≈ **53 MB** RSS;
+ torch/torchvision ≈ **323 MB**; + MobileNetV3-Small instantiated ≈ **336 MB** peak RSS before
serving the first request. A 512 MB free instance fits, with modest headroom — Grad-CAM peaks are
short (empty-cache after every prediction). **Fallback if the free instance OOMs:** Render
Starter ($7/mo) — or split the poller out as a second service on a paid tier.

## 2. Free-tier limits you must state when demoing

| Limit | Vendor-published fact | Mitigation / honest framing |
|---|---|---|
| Web service spin-down | free service sleeps after **15 min idle**; cold start **~1 min** ([Render docs/pricing, 2026](https://render.com/articles/platforms-with-a-real-free-tier-for-developers-in-2026)) | open the URL 2 min before a live demo; the UI loading state is honest |
| Postgres expiry | free DB **expires 30 days** after creation, deleted after a 14-day grace (same source) | the demo dataset is throwaway by design; re-seed is `alembic upgrade head` + `app.db.seed` at boot; note the creation date in the deploy log (§7) |
| Instance hours | **750 free hours/month** per workspace | one 24/7 service fits (744 h); a second free service would not |
| Ephemeral disk | free services have **no persistent disk** — uploads/reports vanish on redeploy | demo uploads are 12 h TTL anyway (demo cleanup); the DB rows flag demo; nothing of record lives on the demo URL |
| RAM | free instances: **512 MB** | measured above; watch the Render metrics tab on first boot |
| Vercel Hobby | non-commercial use only (Vercel ToS) | the demo is a non-commercial evaluation showcase — consistent |

## 3. Deploy steps — operator checklist

### A. Backend (Render, ~10 min)

1. Push this repo as usual (blueprint is `render.yaml` at repo root).
2. Render Dashboard → **New → Blueprint** → connect the GitHub repo. Render reads
   `render.yaml` and offers: `cropmind-demo-api` (web, free) + `cropmind-demo-db` (Postgres, free).
3. Confirm. Render builds `docker/demo.Dockerfile` (~6–10 min first build — torch wheel is large;
   the build log shows it, this is normal).
4. When the service is live, open **Environment** and set the one secret-style value that
   blueprint-sync cannot invent:
   - `CORS_ORIGINS` = the Vercel URL from step B (e.g. `https://cropmind-ai.vercel.app`).
     `JWT_SECRET_KEY` is auto-generated by the blueprint; `DATABASE_URL` is auto-wired from the DB.
5. Health check: Render polls `/health/ready` — expect **Ready** once booted. Check the service
   log tail for the loud line `DEMO model active (...) — predictions are plumbing demos` — that
   line SHOULD be there on the demo URL. If it is absent, a real checkpoint somehow shipped:
   stop and investigate before sharing the URL (AD-008 asks exactly this).

### B. Frontend (Vercel, ~5 min)

1. Vercel Dashboard → **Add New → Project** → import the same GitHub repo.
2. **Root Directory:** `frontend`. Framework preset auto-detects Next.js.
3. **Environment variable:** `NEXT_PUBLIC_API_URL` = the Render URL from step A
   (e.g. `https://cropmind-demo-api.onrender.com`). This is a build-time value — a change
   requires a redeploy (Vercel does this from the same env-var page).
4. Deploy. Open the URL: the landing model-truth strip should read **DEMO** everywhere.
5. Back in Render, ensure `CORS_ORIGINS` matches this exact origin (no trailing slash).

### C. First-boot smoke test (5 min)

1. Register the first account on the public URL (it becomes ADMIN — stated, by design; a
   public demo account is fine since all demo data expires).
2. Upload a leaf photo → analysis → DEMO-flagged result → zones → report PDF.
3. Confirm: prediction payload shows `"demo": true`; PDF demo banner present; exported CSV
   filename carries the SIMULATION label.
4. Paste both the public URL and this smoke-test outcome into the deploy log (§7).

## 4. Vercel configuration file

`frontend/vercel.json` pins the framework and build/install commands so the project config lives
in-repo, not only in a dashboard (files win). Two settings remain dashboard-side by Vercel's
design — set them during import (step B): **Root Directory = `frontend`** and
**`NEXT_PUBLIC_API_URL`** (build-time; changing it needs a redeploy from the env-var page).

## 5. Client behaviour on the free tier (2026-08-11 hardening)

The first live evening surfaced the two failure shapes this document predicts:

- **Cold start** — service asleep after 15 idle minutes; the first request waits ~1 min.
  The web client used to declare "cannot reach the API" on the first blip. It now retries
  bounded (immediate, +10 s, +45 s — the budget covers the published wake time) and the
  Analyze wizard keeps polling across wake-ups, showing a visible "waking up…" note instead
  of ever mislabelling a live analysis as FAILED. A wake is stated, never hidden.
- **Memory squeeze** — torch inference runs in the same free 512 MB process budget as the
  API. If the instance is killed mid-inference, the job's heartbeat goes stale, is reaped and
  retried per the Phase-5 queue rules, and the analysis ends **FAILED with the real error**
  after 3 attempts — records never pretend success. The client retry layer above makes the
  restart window invisible to users; the deploy log records it if it recurs, and §2's
  RAM fallback (paid instance) applies at that point — none of this is silent.

## 6. What is NOT deployed (stated plainly)

- **The real model + real checkpoint.** Public demo runs the sample model by design.
- **The drone provider integrations.** Intent-receipt seams only (docs/02 §8) — no aircraft.
- **TLS hardening beyond platform defaults, custom domain, CDN rules.** Free-tier demos use the
  vendor subdomains and their managed certs.
- **Persistent artifact storage.** Ephemeral per §1 table (stated in the demo script).

## 7. Deploy log (files win — the running record lives here)

| Date (UTC) | URL | Host / shape | Outcome | Notes |
|---|---|---|---|---|
| 2026-08-11 | https://cropmind-demo-api.onrender.com | Render free web (single-process demo image) + free Postgres | first build FAILED (exit 1): inline `#` comment inside a continued ENV line — fixed in `09087a3` and pinned by a test; second build live, `/health/ready` ok | build time ~7 min (torch wheel) |
| 2026-08-11 | https://cropmind-ai-theta.vercel.app | Vercel Hobby — frontend, `NEXT_PUBLIC_API_URL` baked at build | live; CORS origin opened on Render after the URL existed (`Save and deploy`, no rebuild) | demo DB created 2026-08-11 → free Postgres expires ~2026-09-10 (§2) |
| 2026-08-11 | same URLs | client hardening shipped (no infrastructure change) | fixed: single network blip during a cold start surfaced as "cannot reach the API" / mislabelled poll failure | client now retries within the published wake window and shows a "waking up" note; e2e smoke below re-run after this fix |

Record each re-deploy as a row. If a deploy changes behaviour (e.g. RAM fallback to a paid
instance), the change is described here and in the commit message — never only in a dashboard.
